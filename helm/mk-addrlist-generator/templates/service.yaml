apiVersion: v1
kind: Service
metadata:
  name: {{ include "mk-addrlist-generator.fullname" . }}
  labels:
    {{- include "mk-addrlist-generator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mk-addrlist-generator.selectorLabels" . | nindent 4 }}
