version: '3.8'

services:
  mk-addrlist-generator:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8181:8080"
    volumes:
      - ./config.yaml:/app/config.yaml:ro
    # - ./lists:/app/lists:ro  # Optional: for file-based lists
    restart: unless-stopped
    # healthcheck:
    #   test: ["CMD", "wget", "-q", "--spider", "http://localhost:8080/lists/all"]
    #   interval: 30s
    #   timeout: 10s
    #   retries: 3
    #   start_period: 5s